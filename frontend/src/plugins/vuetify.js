import { createVuetify } from "vuetify";
import "vuetify/styles";

export default createVuetify({
  theme: {
    themes: {
      light: {
        primary: "#003366",
        secondary: "#38598a",
        textLink: "#1A5A96",
        red: "#D8292F",
        success: "#2E8540",
      },
    },
  },
});
